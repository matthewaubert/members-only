<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body class="mx-auto px-4 py-4">
    <header class="flex gap-4 justify-between items-center mb-4">
      <h1 class="text-2xl">
        <a href="/">MembersOnly</a>
      </h1>
      <nav class="flex gap-2">
        <% if (!locals.currentUser) { %>
        <a
          href="/signup"
          class="bg-gray-500 hover:bg-gray-600 text-white hover:no-underline rounded px-3 py-2"
        >
          Sign up
        </a>
        <a
          href="/login"
          class="bg-gray-500 hover:bg-gray-600 text-white hover:no-underline rounded px-3 py-2"
        >
          Log in
        </a>
        <% } else { if (!currentUser.member) { %>
        <a
          href="/become-member"
          class="bg-gray-500 hover:bg-gray-600 text-white hover:no-underline rounded px-3 py-2"
        >
          Become a Member
        </a>
        <% } else if (!currentUser.admin) { %>
        <a
          href="/become-admin"
          class="bg-gray-500 hover:bg-gray-600 text-white hover:no-underline rounded px-3 py-2"
        >
          Become an Admin
        </a>
        <% } %>
        <a
          href="/new-message"
          class="bg-gray-500 hover:bg-gray-600 text-white hover:no-underline rounded px-3 py-2"
        >
          New Message
        </a>
        <a
          href="/logout"
          class="bg-gray-500 hover:bg-gray-600 text-white hover:no-underline rounded px-3 py-2"
        >
          Log Out
        </a>
        <% } %>
      </nav>
    </header>
    <main>
      <!-- block content -->
